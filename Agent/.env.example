PORT=8787
HOST=127.0.0.1
CLIENT_ORIGIN=http://localhost:3000

# ── AI model (only ANNA_API_KEY is required) ──────────────────────────────────
# Set ANNA_API_KEY to any OpenAI-compatible key (OpenAI, Azure, Anna AI, etc.)
# Without it the server falls back to the built-in local template.
ANNA_API_KEY=sk-proj-RYRbFWr0XxAjNSOVWXMGPDyw_4uPTTmejCdKSs1QcWLCtqad

# Optional overrides — defaults shown below work for standard OpenAI keys.
# Change ANNA_API_URL if you use a custom/proxied OpenAI-compatible endpoint.
ANNA_API_URL=https://api.openai.com/v1/chat/completions
ANNA_MODEL=gpt-4o
