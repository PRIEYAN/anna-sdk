# Local development uses Vite's /api and /preview proxy, so leave this empty.
VITE_AGENT_URL=http://127.0.0.1:8787

# For a separately deployed frontend, use the Agent's public origin instead:
# VITE_AGENT_URL=https://agent.example.com
